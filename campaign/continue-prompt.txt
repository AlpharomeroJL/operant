Continue the Operant v1.0.0 autonomous build campaign. You are the ORCHESTRATOR. Proceed fully autonomously: do NOT re-plan, do NOT ask questions, do NOT stop until the campaign is complete or usage is exhausted.

Working directory: D:\dev\operant (you are in it). Set the environment variable CARGO_TARGET_DIR=D:\dev\operant-target for all cargo and `just` commands.

FIRST, read these to reload context:
- campaign/RESUME.md (the resume runbook)
- campaign/checkpoint.md (current status, decisions, fix-at-gate log)
- campaign/state.json (the 64-packet ledger)

THEN run this loop until done:
1. Run: node campaign/frontier.mjs  -> lists packets whose dependencies are all merged and which are not yet merged (reconciled from campaign/merged/*.ok).
2. For each dispatchable packet (respect the priority order it prints; backlog trust packets X1,X2,X4,X16 lead once unblocked):
   a. Create its worktree: git worktree add lanes/<id> -b lane/<id> main
   b. Write a tight brief and dispatch a subagent to implement it against the FROZEN contracts in contracts/ and the specs in docs/specs/. Use the tier in campaign/state.json (opus for strong packets L6*/L8*/U4A/X2/X5, haiku for economy, sonnet otherwise) via the Agent tool's model parameter.
   c. IMPORTANT (headless mode): dispatch subagents SYNCHRONOUSLY with run_in_background:false and WAIT for each to finish, because background tasks do not survive a headless session ending. Process several packets per session; a few in parallel is fine only if you await them all before this response ends.
   d. Gate each lane yourself: run `just ci` (or the lane's targeted tests) in the worktree; trust nothing a command can check. Fix-at-gate only for imports, paths, version pins; log each in campaign/checkpoint.md.
   e. Green -> rebase on main, merge --no-ff to main, run `just ci` on main, write campaign/merged/<id>.ok, commit, and `git push` over SSH. Then `git worktree remove lanes/<id> --force` and delete the branch. Red twice -> escalate one model tier; strong-tier red twice -> park with a note in checkpoint.md and move on.
3. Repeat until frontier shows DISPATCHABLE NOW (0).
4. Then run the FINAL GATE from campaign/MEGA_PROMPT.md section 5: full `just ci` green on main; safety regression suite; first-timer path; soak report; assemble README with real assets; write docs/adr/0001-campaign-outcome.md; push both repos; deploy the docs site via the gh-pages subtree method in RESUME.md (NOT GitHub Actions); `gh release create v1.0.0` with the installer, SHA256SUMS, SBOM, and generated notes; re-download and verify the release asset signature; create the ten good-first-issues (X10). When the release is live, write campaign/merged/RELEASED.ok with the release URL.

HARD RULES (never violate):
- Every commit authored `Josef Long <Josefdean@protonmail.com>` with ZERO AI attribution (no Co-Authored-By, no "Generated with"). Repo git config already pins this.
- NO GitHub Actions or .github/workflows. CI is on-device (`just ci`). Docs deploy from the gh-pages branch via subtree push. Remotes are SSH.
- No em dashes anywhere (CI greps U+2014). Contracts are append-only. Default-mode UI strings pass the microcopy lint.
- main only advances on a green `just ci`. Push after every merge so state is durable.
